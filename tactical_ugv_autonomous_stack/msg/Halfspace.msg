# A single linear collision-avoidance constraint row a*x + b*y + c*z <= d, matching one row
# of the legacy comm_server "constraint" wire format ('C' tag + a,b,c,d CSV quadruples).
float64 a
float64 b
float64 c
float64 d
